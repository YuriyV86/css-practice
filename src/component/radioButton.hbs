<div class='form-check'>
  <input
    class={{input-class}}
    type={{input-type}}
    name={{name}}
    id={id}
    {{#if isChecked}}checked{{/if}}
  />
  <label class={{input-label-class}} for={{id}}>
    {{text}}
  </label>
</div>